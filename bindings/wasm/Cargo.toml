[package]
name = "iucn-rle-wasm"
description = "WebAssembly bindings for IUCN Red List of Ecosystems assessment calculations"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
iucn-rle-core = { workspace = true }
serde = { workspace = true }
# Converts the summary into a real JS object rather than a JSON string, so the
# browser API is idiomatic. Small and wasm-native.
serde-wasm-bindgen = "0.6"
wasm-bindgen = "0.2"

[dev-dependencies]
wasm-bindgen-test = "0.3"

[lints]
workspace = true
